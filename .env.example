# Optional: enable live CORTEX INTELLIGENCE chat (Anthropic Messages API).
# Without a key, the copilot falls back to simulated streaming responses.
# WARNING: a key placed here is exposed to the browser bundle. Use only for local demos.
VITE_ANTHROPIC_API_KEY=
